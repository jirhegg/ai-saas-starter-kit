# 사용자 가이드

## 목차

1. [프로젝트 구동하기](#프로젝트-구동하기)
2. [회원가입 및 로그인](#회원가입-및-로그인)
3. [대시보드 사용하기](#대시보드-사용하기)
4. [문서 관리](#문서-관리)
5. [AI 채팅](#ai-채팅)
6. [LLM 설정](#llm-설정)
7. [실제 사용 시나리오](#실제-사용-시나리오)

---

## 프로젝트 구동하기

### 1단계: 환경 설정

```bash
# 저장소 클론
git clone https://github.com/ez2sarang/ai-saas-starter-kit.git
cd ai-saas-starter-kit

# 의존성 설치
npm install
```

### 2단계: 데이터베이스 설정

#### 옵션 A: Supabase 로컬 환경 (권장)

```bash
# Supabase CLI 설치
brew install supabase/tap/supabase

# Supabase 시작
supabase start

# 출력 예시:
# API URL: http://127.0.0.1:54321
# DB URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres
# anon key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

#### 옵션 B: Supabase 클라우드

1. https://supabase.com 에서 프로젝트 생성
2. Settings > API에서 URL과 anon key 복사

### 3단계: 환경 변수 설정

```bash
# .env.local 파일 생성
cp .env.example .env.local
```

`.env.local` 파일 편집:

```env
# Supabase (로컬)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# LLM API 키 (선택 - 로컬 LLM 사용 시 불필요)
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AIza...
CLAUDE_API_KEY=sk-ant-...
```

### 4단계: 데이터베이스 마이그레이션

```bash
# 스키마 적용
npm run db:push

# 성공 메시지:
# ✓ Applying migrations...
# ✓ Done!
```

### 5단계: 개발 서버 실행

```bash
npm run dev

# 출력:
# ▲ Next.js 16.0.3 (Turbopack)
# - Local:        http://localhost:3000
# - Network:      http://192.168.x.x:3000
# ✓ Ready in 336ms
```

브라우저에서 http://localhost:3000 접속!

---

## 회원가입 및 로그인

### 회원가입

1. **홈페이지 접속**
   - http://localhost:3000
   - "무료로 시작하기" 버튼 클릭

2. **회원가입 페이지**
   - 이메일: `test@example.com`
   - 비밀번호: `password123` (최소 6자)
   - "회원가입" 버튼 클릭

3. **이메일 확인**
   - Supabase 로컬 환경: 자동 확인됨
   - Supabase 클라우드: 이메일 확인 링크 클릭

### 로그인

1. **로그인 페이지**
   - http://localhost:3000/login
   - 이메일과 비밀번호 입력
   - "로그인" 버튼 클릭

2. **자동 리다이렉트**
   - 로그인 성공 시 `/dashboard`로 이동
   - 세션은 7일간 유지

---

## 대시보드 사용하기

### 대시보드 화면 구성

```
┌─────────────────────────────────────────────────────┐
│  AI SaaS                                    [사용자] │
├──────────┬──────────────────────────────────────────┤
│          │  대시보드                                 │
│ 대시보드  │  AI 문서 검색 플랫폼에 오신 것을 환영합니다│
│          │                                          │
│ 문서     │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│          │  │  0   │ │  0   │ │  0   │ │  0   │   │
│ 채팅     │  │총문서│ │총대화│ │API호출│ │토큰 │   │
│          │  └──────┘ └──────┘ └──────┘ └──────┘   │
│ 설정     │                                          │
│          │  빠른 시작                                │
│ [로그아웃]│  [문서 업로드] [AI 채팅] [설정]          │
└──────────┴──────────────────────────────────────────┘
```

### 통계 카드

1. **총 문서**: 업로드한 문서 개수
2. **총 대화**: AI와 나눈 대화 수
3. **이번 달 API 호출**: 현재 월의 API 사용량
4. **사용된 토큰**: 총 LLM 토큰 사용량

### 빠른 시작 메뉴

- **문서 업로드**: 새 문서 추가
- **AI 채팅**: 문서에 대해 질문
- **설정**: LLM 제공자 선택

---

## 문서 관리

### 문서 업로드

1. **문서 페이지 이동**
   - 사이드바에서 "문서" 클릭
   - 또는 http://localhost:3000/dashboard/documents

2. **새 문서 만들기**
   ```
   [+ 새 문서] 버튼 클릭
   
   ┌─────────────────────────────────┐
   │ 새 문서 만들기                   │
   ├─────────────────────────────────┤
   │ 제목: [Next.js 16 가이드      ] │
   │                                 │
   │ 내용:                           │
   │ ┌─────────────────────────────┐ │
   │ │ Next.js 16은 Turbopack을   │ │
   │ │ 기본 번들러로 사용합니다.   │ │
   │ │                             │ │
   │ │ 주요 변경사항:              │ │
   │ │ - middleware → proxy       │ │
   │ │ - React 19 지원            │ │
   │ └─────────────────────────────┘ │
   │                                 │
   │ [생성] [취소]                   │
   └─────────────────────────────────┘
   ```

3. **문서 생성 완료**
   - "생성" 버튼 클릭
   - 문서 목록에 추가됨
   - 상태: "완료" (초록색)

### 문서 목록 보기

```
┌──────────────────────────────────────────────────┐
│ 문서                              [+ 새 문서]    │
│ 문서를 업로드하고 AI로 검색하세요                 │
├──────────────────────────────────────────────────┤
│                                                  │
│ ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│ │ 📄         │ │ 📄         │ │ 📄         │   │
│ │            │ │            │ │            │   │
│ │ Next.js 16 │ │ React 19   │ │ TypeScript │   │
│ │ 가이드     │ │ 새 기능    │ │ 타입 정의  │   │
│ │            │ │            │ │            │   │
│ │ Next.js 16 │ │ React 19의 │ │ TypeScript │   │
│ │ 은 Turbo...│ │ 주요 변경...│ │ 5.3의 새...│   │
│ │            │ │            │ │            │   │
│ │ 2024-11-21 │ │ 2024-11-21 │ │ 2024-11-20 │   │
│ │ [완료] [🗑]│ │ [완료] [🗑]│ │ [완료] [🗑]│   │
│ └────────────┘ └────────────┘ └────────────┘   │
└──────────────────────────────────────────────────┘
```

### 문서 삭제

1. 문서 카드의 🗑️ 아이콘 클릭
2. Soft delete (deleted_at 설정)
3. 목록에서 사라짐

---

## AI 채팅

### 채팅 시작하기

1. **채팅 페이지 이동**
   - 사이드바에서 "채팅" 클릭
   - 또는 http://localhost:3000/dashboard/chat

2. **채팅 화면**
   ```
   ┌──────────────────────────────────────────────┐
   │ AI 채팅                                      │
   │ 문서에 대해 질문하고 AI의 답변을 받아보세요   │
   ├──────────────────────────────────────────────┤
   │                                              │
   │                                              │
   │                                              │
   │              대화를 시작해보세요!             │
   │                                              │
   │                                              │
   │                                              │
   ├──────────────────────────────────────────────┤
   │ [메시지를 입력하세요...            ] [전송]  │
   └──────────────────────────────────────────────┘
   ```

### 대화 예시

**사용자 입력:**
```
Next.js 16의 주요 변경사항은 무엇인가요?
```

**AI 응답:**
```
┌──────────────────────────────────────────────┐
│ Next.js 16의 주요 변경사항은 무엇인가요?  [나]│
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ [AI]                                         │
│                                              │
│ Next.js 16의 주요 변경사항은 다음과 같습니다: │
│                                              │
│ 1. **Turbopack 기본 번들러**                 │
│    - Webpack 대비 700배 빠른 성능            │
│    - 개발 서버 시작 시간 단축                │
│                                              │
│ 2. **Proxy 시스템**                          │
│    - middleware.ts → proxy.ts로 변경        │
│    - 동일한 기능, 새로운 이름                │
│                                              │
│ 3. **React 19 지원**                         │
│    - Server Components 개선                 │
│    - 자동 메모이제이션                       │
│                                              │
│ 더 자세한 내용이 필요하신가요?               │
└──────────────────────────────────────────────┘
```

### 대화 기록

- 모든 대화는 `chat_history` 테이블에 저장
- 사용자 메시지와 AI 응답 모두 기록
- 토큰 사용량 추적

---

## LLM 설정

### 설정 페이지

1. **설정 페이지 이동**
   - 사이드바에서 "설정" 클릭
   - 또는 http://localhost:3000/dashboard/settings

2. **LLM 제공자 선택**
   ```
   ┌─────────────────────────────────────────┐
   │ ⚙️ 설정                                 │
   │ AI 모델 및 API 키를 설정하세요           │
   ├─────────────────────────────────────────┤
   │                                         │
   │ LLM 제공자                              │
   │ ┌─────────────────────────────────────┐ │
   │ │ LM Studio (로컬)              ▼    │ │
   │ └─────────────────────────────────────┘ │
   │                                         │
   │ 모델                                    │
   │ ┌─────────────────────────────────────┐ │
   │ │ Kimi K2 Thinking              ▼    │ │
   │ └─────────────────────────────────────┘ │
   │                                         │
   │ LM Studio Base URL                     │
   │ ┌─────────────────────────────────────┐ │
   │ │ http://localhost:1234              │ │
   │ └─────────────────────────────────────┘ │
   │                                         │
   │ [💾 설정 저장]                          │
   └─────────────────────────────────────────┘
   ```

### LLM 제공자별 설정

#### 1. LM Studio (로컬 - 권장)

**장점:**
- ✅ API 키 불필요
- ✅ 완전 무료
- ✅ 데이터 프라이버시
- ✅ 오프라인 사용 가능

**설정 방법:**
```bash
# 1. LM Studio 다운로드
# https://lmstudio.ai/

# 2. 모델 다운로드
# - Kimi K2 Thinking
# - Llama 3.1
# - Mistral 등

# 3. 로컬 서버 시작
# LM Studio > Local Server > Start Server
# 포트: 1234
```

**설정 화면:**
- LLM 제공자: `LM Studio (로컬)`
- 모델: `Kimi K2 Thinking`
- Base URL: `http://localhost:1234`

#### 2. Ollama (로컬)

**설정 방법:**
```bash
# 1. Ollama 설치
brew install ollama

# 2. 모델 다운로드
ollama pull llama2
ollama pull mistral

# 3. 서버 시작 (자동)
ollama serve
```

**설정 화면:**
- LLM 제공자: `Ollama (로컬)`
- 모델: `llama2`
- Base URL: `http://localhost:11434`

#### 3. OpenAI

**설정 방법:**
```bash
# 1. API 키 발급
# https://platform.openai.com/api-keys

# 2. 설정 화면에서 입력
```

**설정 화면:**
- LLM 제공자: `OpenAI`
- 모델: `GPT-4 Turbo`
- API Key: `sk-...`

#### 4. Google Gemini

**설정 방법:**
```bash
# 1. API 키 발급
# https://makersuite.google.com/app/apikey

# 2. 설정 화면에서 입력
```

**설정 화면:**
- LLM 제공자: `Google Gemini`
- 모델: `Gemini Pro`
- API Key: `AIza...`

#### 5. Claude

**설정 방법:**
```bash
# 1. API 키 발급
# https://console.anthropic.com/

# 2. 설정 화면에서 입력
```

**설정 화면:**
- LLM 제공자: `Claude`
- 모델: `Claude 3.5 Sonnet`
- API Key: `sk-ant-...`

---

## 실제 사용 시나리오

### 시나리오 1: 기술 문서 Q&A 시스템

**목표:** 회사 내부 기술 문서를 AI로 검색

**단계:**

1. **문서 업로드**
   ```
   제목: Next.js 배포 가이드
   내용:
   # Vercel 배포 방법
   1. GitHub 저장소 연결
   2. 환경 변수 설정
   3. 배포 버튼 클릭
   
   # 환경 변수
   - NEXT_PUBLIC_API_URL
   - DATABASE_URL
   - STRIPE_SECRET_KEY
   ```

2. **AI에게 질문**
   ```
   Q: Vercel에 배포할 때 필요한 환경 변수는?
   
   A: Vercel 배포 시 다음 환경 변수가 필요합니다:
   1. NEXT_PUBLIC_API_URL - API 엔드포인트
   2. DATABASE_URL - 데이터베이스 연결 문자열
   3. STRIPE_SECRET_KEY - Stripe 결제 키
   
   이 변수들은 Vercel 대시보드의 Settings > 
   Environment Variables에서 설정할 수 있습니다.
   ```

3. **결과**
   - 문서를 일일이 찾지 않아도 됨
   - 즉시 답변 획득
   - 컨텍스트 기반 답변

### 시나리오 2: 코드 리뷰 도우미

**목표:** 코드 베스트 프랙티스 학습

**단계:**

1. **문서 업로드**
   ```
   제목: React 베스트 프랙티스
   내용:
   # Hooks 규칙
   - 최상위에서만 호출
   - React 함수 내에서만 호출
   - 조건문 안에서 호출 금지
   
   # 성능 최적화
   - useMemo로 비싼 계산 메모이제이션
   - useCallback으로 함수 메모이제이션
   - React.memo로 컴포넌트 메모이제이션
   ```

2. **AI에게 질문**
   ```
   Q: useEffect를 조건문 안에서 사용하면 안 되는 이유는?
   
   A: useEffect를 조건문 안에서 사용하면 안 되는 이유는:
   
   1. **Hooks 규칙 위반**
      - Hooks는 항상 같은 순서로 호출되어야 함
      - 조건문은 실행 순서를 변경할 수 있음
   
   2. **React의 내부 상태 관리**
      - React는 Hooks 호출 순서로 상태를 추적
      - 순서가 바뀌면 상태가 꼬일 수 있음
   
   3. **올바른 방법**
      - useEffect 내부에 조건문 사용
      - 예: useEffect(() => { if (condition) {...} }, [])
   ```

### 시나리오 3: 온보딩 가이드

**목표:** 신입 개발자 온보딩 자동화

**단계:**

1. **여러 문서 업로드**
   - 개발 환경 설정
   - Git 워크플로우
   - 코딩 컨벤션
   - 배포 프로세스

2. **신입 개발자 질문**
   ```
   Q: 새 기능을 개발할 때 어떤 순서로 진행하나요?
   
   A: 새 기능 개발 순서는 다음과 같습니다:
   
   1. **브랜치 생성**
      - git checkout -b feature/기능명
   
   2. **개발**
      - 코딩 컨벤션 준수
      - 타입 안전성 확인
   
   3. **테스트**
      - npm run test
      - npm run lint
   
   4. **커밋**
      - feat: 기능 설명 형식
   
   5. **PR 생성**
      - PR 템플릿 작성
      - 리뷰어 지정
   
   6. **배포**
      - main 브랜치 머지 후 자동 배포
   ```

### 시나리오 4: API 문서 검색

**목표:** API 사용법 빠르게 찾기

**단계:**

1. **API 문서 업로드**
   ```
   제목: REST API 문서
   내용:
   # POST /api/users
   사용자 생성
   
   Request:
   {
     "email": "user@example.com",
     "name": "John Doe"
   }
   
   Response:
   {
     "id": "uuid",
     "email": "user@example.com",
     "created_at": "2024-11-21T..."
   }
   ```

2. **개발 중 질문**
   ```
   Q: 사용자를 생성하는 API 엔드포인트는?
   
   A: 사용자 생성 API는 다음과 같습니다:
   
   **엔드포인트:** POST /api/users
   
   **요청 본문:**
   ```json
   {
     "email": "user@example.com",
     "name": "John Doe"
   }
   ```
   
   **응답:**
   ```json
   {
     "id": "uuid",
     "email": "user@example.com",
     "created_at": "2024-11-21T..."
   }
   ```
   ```

---

## 팁과 트릭

### 1. 로컬 LLM 추천

**개발/테스트용:**
- LM Studio + Kimi K2 Thinking
- 빠른 응답 속도
- 무료

**프로덕션용:**
- OpenAI GPT-4 Turbo
- 높은 품질
- 안정적

### 2. 문서 작성 팁

**좋은 문서:**
```markdown
# 명확한 제목
## 구조화된 섹션
- 구체적인 예시
- 코드 블록 포함
```

**나쁜 문서:**
```
그냥 텍스트만 쭉...
구조 없음...
예시 없음...
```

### 3. 질문 작성 팁

**좋은 질문:**
- "Next.js 16에서 middleware를 proxy로 마이그레이션하는 방법은?"
- "TypeScript에서 제네릭 타입을 사용하는 예시는?"

**나쁜 질문:**
- "이거 어떻게 해?"
- "에러 났어"

### 4. 성능 최적화

**문서 크기:**
- 한 문서당 10,000자 이하 권장
- 너무 크면 여러 문서로 분할

**토큰 절약:**
- 간결한 질문
- 필요한 정보만 요청

---

## 문제 해결

### 문제 1: 로그인이 안 돼요

**확인 사항:**
1. Supabase가 실행 중인가?
   ```bash
   supabase status
   ```

2. 환경 변수가 올바른가?
   ```bash
   cat .env.local
   ```

3. 이메일 확인을 했는가?

### 문제 2: AI 응답이 안 와요

**확인 사항:**
1. LLM 설정이 되어 있는가?
   - 설정 페이지 확인

2. 로컬 LLM이 실행 중인가?
   ```bash
   # LM Studio: GUI에서 확인
   # Ollama: 
   ollama list
   ```

3. API 키가 유효한가?
   - OpenAI/Gemini/Claude 대시보드 확인

### 문제 3: 문서가 안 보여요

**확인 사항:**
1. 데이터베이스 연결 확인
   ```bash
   npm run db:studio
   ```

2. 브라우저 콘솔 확인
   - F12 > Console 탭

3. 서버 로그 확인
   - 터미널에서 에러 메시지 확인

---

## 도움이 필요하신가요?

- **기술 문의**: sales@com.dooray.com
- **GitHub Issues**: https://github.com/ez2sarang/ai-saas-starter-kit/issues
- **문서**: [프로젝트 히스토리](PROJECT_HISTORY.md), [기술 스택](tech-stack/)

---

**Happy Coding!**
